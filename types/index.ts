export type TimelineItem = {
  title: string;
  date: string;
  body: string;
};

export type ScoreboardItem = {
  label: string;
  husband: string;
  wife: string;
  husbandValue: number;
  wifeValue: number;
};

export type GalleryImage = {
  src: string;
  alt: string;
  heightClass: string;
};
